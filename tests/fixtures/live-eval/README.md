# Pricing fixture

`summarizeCart(lines, options)` returns monetary values rounded to two decimals.
Discounts are applied before tax, and the public return shape must stay stable.

Run `npm test` to verify the behavior.
